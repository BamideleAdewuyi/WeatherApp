class AppController {
    constructor(model, view) {
        this.model = model;
        this.view = view;
    }

    handleSearchLocation(location) {
        this.model
    }
}

export default AppController;